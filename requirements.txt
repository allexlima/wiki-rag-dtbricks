# Root project dependencies (local development).
# Includes src/ runtime deps + extras for notebooks.
# Pinned to exact versions for deterministic builds.
#
# Install: uv pip install -r requirements.txt

-r src/requirements.txt
Pillow==12.1.1
