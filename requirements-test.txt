# Test dependencies.
# Pinned to exact versions for deterministic CI builds.
#
# Install: uv pip install -r requirements-test.txt

pytest==9.0.2
pytest-cov==7.0.0
pytest-mock==3.15.1
responses==0.26.0
